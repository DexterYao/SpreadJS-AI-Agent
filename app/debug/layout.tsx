import type { Metadata } from "next";

export const metadata: Metadata = {
	title: "Debug Tool Call | SpreadJS AI Agent",
};

export default function DebugLayout({ children }: { children: React.ReactNode }) {
	return children;
}
